# ============================================================
# Ejecuta todos los tests (unit, integración, e2e y humo)
# ============================================================
test:
	pytest -v --disable-warnings --cov=src --cov-report=term-missing
